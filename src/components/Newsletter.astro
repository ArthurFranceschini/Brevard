---
---
<section id="signup" class="section text-primary-50 dark:text-primary-500 bg-primary-500 dark:bg-primary-50">
  <div class="container-custom">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="col-span-full">
        <div class="card p-8 md:p-12 border border-primary-50 dark:border-gray-700">
          <div class="text-left mb-12">
            <h2 class="mb-6">Fique por dentro do que faz as marcas crescerem.</h2>
            <p class="text-left text-primary-50 dark:text-primary-500">
              Inscreva-se em nossa newsletter para receber as novidades!
            </p>
          </div>
          
          <form class="w-full" x-data="{ email: '', submitted: false, error: '' }">
            <div class="relative" x-show="!submitted">
              <div class="flex flex-col sm:flex-row gap-4 justify-between">
                <div class="w-full">
                  <label for="email" class="sr-only">Seu E-mail</label>
                  <input 
                    type="email" 
                    id="email" 
                    x-model="email"
                    placeholder="Seu E-mail" 
                    class="w-full px-4 py-4 text-lg rounded-lg border border-primary-50 dark:border-primary-200 bg-primary-500 text-primary-50 focus:outline-hidden focus:ring-2 focus:ring-primary-500 dark:focus:ring-primary-400"
                    required
                  />
                  <p class="text-red-500 text-sm mt-1" x-show="error" x-text="error"></p>
                </div>
                <button 
                  type="submit" 
                  class="btn-primary whitespace-nowrap"
                  x-on:click.prevent="
                    if (!email) {
                      error = 'Please enter your email';
                      return;
                    }
                    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
                      error = 'Please enter a valid email';
                      return;
                    }
                    error = '';
                    submitted = true;
                  "
                >
                  Inscreva-se
                </button>
              </div>
            </div>
            
            <div class="text-center p-6 bg-primary-50 dark:bg-primary-900/30 rounded-lg" x-show="submitted" x-cloak>
              <svg class="w-12 h-12 text-primary-500 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">Obrigado por se inscrever!</h3>
              <p class="text-gray-600 dark:text-gray-300">
                Inscrição confirmada! Você agora faz parte da nossa newsletter e receberá as últimas novidades e ofertas.
              </p>
            </div>
          </form>
          
          <div class="mt-8 text-center text-sm text-gray-500 dark:text-gray-400">
            Ao se inscrever, você concorda com nossa <a href="#" class="text-accent-50 hover:underline">Política de Privacidade</a> e nossos <a href="#" class="text-accent-50 hover:underline">Termos & Condições</a>.
          </div>
        </div>
      </div>
    </div>
  </div>
</section>